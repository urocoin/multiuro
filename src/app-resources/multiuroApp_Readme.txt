The MultiUroSkeleton.app contains the skeleton to produce the MultiUro Mac application bundle.

To flesh it out it needs:

Everything in the staging directory to be inserted into the:
   MultiUroSkeleton/Contents/Resources/Java directory

